-- ============================================================
-- 자산 단위 환산: 백만원 → 억원 (1회만 실행!)
-- Supabase > SQL Editor 에 붙여넣고 Run
--
-- 예: 70,000(백만원) → 700(억원)
-- ⚠️ 반드시 한 번만 실행하세요. 두 번 실행하면 값이 다시 100으로
--    나눠져서 잘못된 숫자가 됩니다.
-- ============================================================

update public.clients
set aum = round(aum / 100.0, 2)
where aum is not null;
